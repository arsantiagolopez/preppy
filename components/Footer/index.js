import { Flex } from "@chakra-ui/react";
import React from "react";

const Footer = () => {
  return <Flex {...styles.wrapper}></Flex>;
};

export { Footer };

// Styles

const styles = {
  wrapper: {},
};
